'use client'

import React from 'react'

const AddToCart = () => {
  return (
    <>
        <button className='btn btn-primary hover:btn-info' onClick={() => console.log("Clicked")}>Click to add to card</button>
    </>
  )
}

export default AddToCart